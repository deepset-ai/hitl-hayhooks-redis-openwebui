# Hayhooks Server Dependencies
# (Open WebUI pipe dependencies are installed via frontmatter in open-webui-pipe.py)

hayhooks>=1.7.0
redis[hiredis]
git+https://github.com/deepset-ai/haystack-experimental.git
